{
  "name": "oas_to_proxy",
  "version": "1.0.0",
  "description": "This npm package converts the OAS file to the JSON and later stringifies it so that it can be  used inside JSON validator JavaScript within the proxy.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	"start": "node index.js"
  },
  "author": "anshul.x.aggarwal@accenture.com",
  "license": "ISC",
  "dependencies": {
    "copyfiles": "^2.1.1",
    "js-yaml": "^3.13.1"
  }
}
